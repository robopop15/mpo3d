import AppKit

enum PreviewCanvasPalette {
    static let background = NSColor(
        calibratedRed: 158.0 / 255.0,
        green: 158.0 / 255.0,
        blue: 158.0 / 255.0,
        alpha: 1.0
    )
}
